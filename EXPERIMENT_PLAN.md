# Aurora DSQL 중심 비교 실험 계획

> 상태: planned · 작성/공식 문서 확인: 2026-09-24
> 핵심 목적: DSQL이 기존 인스턴스 기반·서버리스 서비스 대비 제공하는 성능과 개발·운영 편의성을 정량 비교한다.
> 업무 우선순위: ① 일반 웹서비스·SaaS OLTP → ② 트래픽 급증·유휴 → ③ 대용량 조회·집계. 비용은 모든 단계에서 측정한다.
> 기본 가정: 서울 리전, PostgreSQL 계열. 실제 서비스의 트래픽·SLO·예산은 아직 지정되지 않았다.

## 1. 실험으로 결정할 것

**DSQL을 도입하면 같은 업무에서 성능이 얼마나 달라지고, 개발·운영 작업이 얼마나 줄거나 늘어나는가?** 다음 질문에 DSQL과 각 대조군의 차이·비율로 답한다. DSQL의 우위를 전제하지 않는다.

1. 같은 요청률에서 DSQL의 p95/p99 지연과 오류율은 얼마이며, SLO를 지키는 처리량은 각 대조군의 몇 배인가?
2. 연결·트래픽 급증, 유휴 후 재개, 인기 상품 경합에서 DSQL은 어떤 지연과 재시도 부담을 보이는가?
3. 최초 연결부터 스키마 배포·용량 대응·문제 진단·복구·삭제까지 작업 시간과 수동 개입은 얼마나 달라지는가?
4. PostgreSQL 업무를 DSQL로 옮기기 위해 필요한 SQL·인증·트랜잭션 변경은 무엇이며, 운영 편의성의 이점과 어떤 맞교환이 있는가?
5. 동일한 업무·정합성·SLO·가용성 요구를 충족할 때 성공 업무당 비용과 전체 실험 비용은 얼마인가?

DSQL의 대조군은 PostgreSQL 계열로 제한한다. 첫 비교부터 **D1/R1/A1/A2**를 포함하고, 기존 서비스끼리의 세부 튜닝 비교는 DSQL 채택 판단에 필요한 경우에만 확대한다. MySQL·분석 전용 DB·NoSQL·상용 엔진의 비교와 다중 리전은 후속 범위다. 단일 리전 결과로 DSQL의 다중 리전 성능·편의성을 일반화하지 않는다.

이 문서는 실험 설계다. 인프라, 부하 도구, 측정 결과는 아직 없으며 아래 설정과 합격 기준은 실행 전에 고정할 제안값이다.

## 2. 실험 대상 서비스와 구성

기존 공식 문서 조사에 사용자의 DSQL 중심 목적을 반영해 비교군을 재선정했다. DSQL 관련 SQL·동시성·인증·복원·과금 문서를 다시 확인했으며 근거는 [서비스 선정 기록](SERVICE_SELECTION.md)에 남겼다.

| ID | 대상 | 제안하는 시작 구성 | 비교 목적 |
| --- | --- | --- | --- |
| D1 | Amazon Aurora DSQL — 단일 리전 클러스터 | 서울 리전, 서비스 관리 용량 | **주 대상·필수.** 모든 핵심 실험의 평가 대상 |
| R1 | Amazon RDS for PostgreSQL — Multi-AZ DB 인스턴스 | `db.r6g.xlarge` primary + 다른 AZ의 standby | **필수.** 전통적인 인스턴스 기반 HA 운영 대비 차이 |
| A1 | Amazon Aurora PostgreSQL-Compatible — Provisioned | `db.r6g.xlarge` writer 1 + 다른 AZ의 reader 1, Aurora Standard | **필수.** 고정 용량 Aurora 대비 성능·관리 작업 차이 |
| A2 | Amazon Aurora PostgreSQL-Compatible — Serverless v2 | writer 1 + 다른 AZ의 reader 1, 각각 4–32 ACU, reader promotion tier 1 | **필수.** 기존 서버리스 대비 자동 확장·유휴·운영 편의성 차이 |
| R2 | Amazon RDS for PostgreSQL — Multi-AZ DB 클러스터 | `db.r6gd.xlarge` writer 1 + reader 2, 3개 AZ | 선택 확장. 읽기 확장·복구 방식이 DSQL 채택 판단에 영향을 줄 때 |
| A3 | Amazon Aurora PostgreSQL-Compatible — Provisioned + Optimized Reads | `db.r6gd.xlarge` writer 1 + 다른 AZ의 reader 1 | 선택 확장. 조회·집계에서 NVMe 효과를 분리할 때 |
| R0 | Amazon RDS for PostgreSQL — Single-AZ | R1과 같은 인스턴스·스토리지 | 필요할 때만 고가용성 구성의 추가 비용·지연을 분리하는 대조군 |

R1의 standby는 읽기에 사용할 수 없고 R2의 reader는 사용할 수 있다. R1에 읽기 복제본을 추가하는 경우 `R1+RR`이라는 별도 구성으로 기록한다. [AWS Multi-AZ 배포 방식](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.MultiAZ.html)

### 구성 고정 규칙

- 리전은 `ap-northeast-2`로 한다. R2의 PostgreSQL 16 지원은 공식 리전 표에서 확인했지만, **정확한 버전·클래스의 주문 가능 여부는 실행 계정에서 재확인**한다. `describe-db-engine-versions`와 `describe-orderable-db-instance-options` 결과를 저장한다. [리전/엔진 지원](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.RDS_Fea_Regions_DB-eng.Feature.MultiAZDBClusters.html)
- R1/R2/A1/A2/A3는 PostgreSQL 16 계열로 맞춘다. 양 서비스가 제공하는 최신 공통 minor를 우선하고, 일치하지 않으면 각 최신 지원 minor와 차이를 기록한다. Aurora 자체 패치와 Serverless 플랫폼 버전도 남긴다. 자동 pause 실험은 이를 지원하는 버전을 사용한다. 최신 major 성능을 평가하는 실험은 아니다.
- 고정 용량의 목표는 **노드당 4 vCPU·32 GiB**다. 제안 클래스의 실제 사양과 지원 여부를 확인한다. R2의 로컬 NVMe 등 차이는 통제되지 않는 변수로 남긴다. 클래스 교체 시 비교군 전체의 세대·메모리 조건을 다시 검토한다. [RDS 사양](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.DBInstanceClass.Summary.html), [Aurora 사양](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Concepts.DBInstanceClass.Summary.html)
- R1/R2의 시작 스토리지는 gp3 400 GiB·12,000 IOPS를 후보로 한다. R1 처리량은 500 MiB/s, R2는 서비스가 IOPS에 따라 정하는 실제 처리량을 기록한다. 미지원 조합을 임의로 대체하지 않는다. gp3의 용량 구간별 성능 변화도 함께 기록한다. [RDS 스토리지](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Storage.html)
- Aurora는 Standard로 시작하고 I/O-Optimized는 E010에서 분리한다. 공유 스토리지와 EBS를 같은 IOPS라고 가정하지 않는다.
- A3와 R1의 NVMe 변형 `R1-NVMe`는 E012/E010의 제한된 조건에서 사용한다. R2도 Optimized Reads 효과가 있을 수 있으므로 임시 공간 설정을 기록한다. Aurora는 A1/A1-IO/A3/A3-IO의 2×2 비교로 NVMe와 스토리지 구성 변경을 분리한다. [RDS Optimized Reads](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_PostgreSQL.optimizedreads.html), [Aurora Optimized Reads](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/AuroraPostgreSQL.optimized.reads.html)
- ACU는 고정 인스턴스의 vCPU와 일대일 대응하지 않는다. A2는 같은 요청과 SLO를 기준으로 비교하고, writer와 reader의 ACU를 각각 적분해 비용을 구한다. tier 1 reader의 용량은 writer 영향을 받는다. [ACU 정의](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/aurora-serverless-v2.how-it-works.html), [reader 용량 정책](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/aurora-serverless-v2-administration.html)
- D1은 vCPU·메모리·ACU를 동일하게 맞출 수 있는 구성이 아니다. 서비스가 관리하는 용량과 DPU 과금 구조를 고려해 같은 업무·요청률·SLO로 비교한다. 실행 시점의 쿼터와 제공 지표를 저장하고 내부 용량을 추측하지 않는다. [DSQL 구조](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/what-is-aurora-dsql.html), [쿼터](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/CHAP_quotas.html), [과금](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/billing-metering.html)
- HA 구성의 전체 노드·스토리지 비용을 비교한다. R0를 HA 구성과 같은 운영 대안으로 순위 매기지 않는다. D1은 클러스터 엔드포인트에, R1/A1/A2는 E005 외에는 writer에 요청을 보낸다. HA의 구현과 관측 가능한 장애 범위가 다르므로 같은 가용성을 실증했다고 표현하지 않는다.

## 3. 공통 데이터와 업무 부하

### 데이터 모델

합성 데이터로 `tenants`, `customers`, `products`, `inventory`, `orders`, `order_items`, `ledger`, `operation_receipts`를 구성한다. PK/FK, 고유 제약, 재고 CHECK, 고객별 주문 검색용 복합 인덱스를 포함한다. 금액은 정수 최소 단위로 저장한다. `operation_receipts`의 고유 업무 ID로 중복 처리와 커밋 여부를 확인한다.

- **S:** 기준 PostgreSQL 구성에서 테이블+인덱스 약 5 GiB. 작은 데이터셋.
- **L:** 같은 생성 규칙으로 약 100 GiB. 고정 용량 기준선의 노드 메모리보다 큰 데이터셋. D1의 메모리·캐시 크기는 가정하지 않는다.
- 생성 시드, 행 수, 값 분포, 스키마·쿼리 해시를 고정한다. 서비스별 물리 저장 크기는 측정값이며, 같은 GiB를 맞추려고 행 수를 바꾸지 않는다.
- 기본은 균등 접근이다. 경합 실험은 인기 상품 1%에 접근의 80%를 집중한다. 쓰기 실험의 반복마다 동일한 초기 상태를 복원한다.

### 업무 유형과 트래픽

| 업무 | 트랜잭션 내용 | 읽기 중심 | 기본 OLTP | 쓰기 중심 |
| --- | --- | ---: | ---: | ---: |
| 상품 조회 | PK/인덱스 조회 | 70% | 40% | 20% |
| 주문 이력 | 고객별 범위 조회, JOIN, 정렬, 페이지 크기 20 | 25% | 30% | 10% |
| 주문 생성 | 재고 조건부 차감 + 주문/항목/영수증을 한 트랜잭션으로 저장 | 3% | 20% | 50% |
| 취소/상태 변경 | 기존 업무 상태 검증 후 변경·재고 반환 | 2% | 10% | 20% |

비율은 SQL 문장 수가 아닌 **업무 요청 수** 기준이다. 재고·취소 대상은 정상 부하에서 고갈되지 않도록 생성한다. 의도된 품절·중복·업무 거절은 별도 실험으로 표시한다. 대용량 JOIN·집계·JSON 조회는 E012에서 별도로 측정하고, E008에서는 운영 중 간섭을 조사한다. 전체 애플리케이션 HTTP 응답시간과 DB 작업 응답시간은 구분한다.

## 4. 측정 방법과 판정 기준

### 환경과 도구

- 같은 리전의 전용 EC2 부하 발생기를 사용하고 모든 비교군에 동일한 사양·이미지·클라이언트 버전을 적용한다. 최초 후보는 비버스터블 8 vCPU급이며 CPU·네트워크 병목이 생기면 발생기를 늘리고 해당 조건을 다시 측정한다.
- VPC/AZ 경로, 엔드포인트, 관측 가능한 writer AZ, RTT를 기록한다. 실패 전환 후 AZ 변경도 기록한다. TLS 인증서 검증, 드라이버, 연결 풀, prepared statement 방식, 세션 설정을 고정하고 서비스별 인증·접속 어댑터 차이를 공개한다. D1의 내부 노드/AZ를 임의 추정하지 않는다.
- 주 비교에는 동일한 Python/psycopg 업무 실행기를 사용해 모든 서비스의 연결·인증·전체 트랜잭션 재시도를 같은 기준으로 측정한다. `pgbench`의 사용자 정의 SQL은 단일 시도 SQL 성능을 살피는 보조 검사로만 사용하며 주 업무 TPS와 섞지 않는다. 인증 토큰 갱신과 서비스별 SQL 어댑터는 같은 실행기 안에서 분리·버전 관리한다. 기본 pgbench 결과를 TPC-C 공식 점수로 표현하지 않는다. [pgbench](https://www.postgresql.org/docs/16/pgbench.html), [psycopg 동시성](https://www.psycopg.org/psycopg3/docs/advanced/async.html), [DSQL 인증](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/authentication-authorization.html)
- DB 세션 통계와 실행 계획, CloudWatch, CloudWatch Database Insights를 수집한다. 확장·통계 뷰 지원은 E001에서 확인한다. 지원하지 않는 지표는 0 대신 `N/A`로 표시한다. 모니터링 모드·주기·비용도 동일하게 기록한다. [Database Insights](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_DatabaseInsights.html)

### 반복과 통제

1. 2분 준비 + 5분 측정으로 동시 연결 16/64/256의 예비 탐색을 한다. 예비값으로 최종 우열을 판단하지 않는다.
2. 본 측정은 **10분 워밍업 + 20분 수집, 조건당 독립 3회**로 시작한다. 워밍업 중 지연·캐시 상태가 안정되지 않으면 연장한다. 장기·장애·복원 실험은 아래 별도 시간을 따른다.
3. 시간대와 실행 순서를 바꾸고 초기 데이터·캐시 준비 절차를 맞춘다. 재부팅만으로 완전한 cold cache를 만들었다고 주장하지 않는다. 최초 접근 성능은 별도로 기록한다.
4. 각 반복의 TPS와 p50/p95/p99, 최솟값·최댓값을 남긴다. p99들을 평균 내어 전체 p99로 표시하지 않는다. 요청 유형별 히스토그램과 표본 수를 보관한다.
5. 같은 조건의 결과 편차가 10%를 넘으면 원인을 점검하고 추가 2회 측정한다. 소수 반복의 범위를 신뢰구간으로 부르지 않는다. 수집 누락·발생기 포화로 무효화한 실행과 이유도 남긴다.

### 사용자 관점의 지표

- 응답 지연: 요청 예정 시각부터 응답까지의 시간. 연결 풀 대기, 연결 수립, SQL 실행, 커밋, 재시도 대기를 별도로 분해한다.
- 처리량: 완료된 고유 업무/초, 기한 내 성공 업무/초, 시도 횟수/초. 재시도와 SQL 문장 수를 성공 TPS에 중복 합산하지 않는다.
- 오류: 최종 실패/전체 예정 요청, timeout, 접속 실패, 교착, 직렬화 실패, 미발행·누락 요청을 구분한다. 성공 요청 지연만 보고 실패를 숨기지 않는다.
- 내부 상태: CPU, 메모리, 연결 수, 대기 이벤트, lock wait, IOPS·처리량·지연, WAL/임시 파일, 복제 지연, 캐시 적중률, ACU 및 DPU 중 제공되는 지표.

고정 동시성 탐색 후 도착률을 고정하는 open-loop 부하를 사용한다. 공통 업무 실행기는 DB 응답과 독립적으로 요청 예정 시각을 생성하고 예정·발행·완료 시각, 대기열 지연, 기한 초과로 미발행된 요청을 기록한다. 시스템이 느려져도 예정 요청 수를 줄이지 않으며 skipped는 실패에 포함한다. 보조 pgbench 검사는 `--rate`의 schedule lag와 `--latency-limit`의 skipped를 별도로 수집한다. [도착률과 지연 측정](https://www.postgresql.org/docs/16/pgbench.html)

| 항목 | 실험용 초기 기준 | 해석 |
| --- | --- | --- |
| 읽기 업무 | p95 ≤ 50 ms, p99 ≤ 100 ms | 상품 조회와 주문 이력 각각 적용 |
| 쓰기 업무 | p95 ≤ 100 ms, p99 ≤ 200 ms | 커밋·풀 대기·재시도 포함 |
| 최종 기술 실패율 | ≤ 0.1% | timeout·skipped 포함; 업무 거절은 따로 보고 |
| OLTP 요청 마감 | 2초 | 초과 요청은 지연 분포와 별도로 실패 처리; 집계는 E012 기준 |
| 정합성 | 업무 불변식 위반·중복 효과·확인된 성공 커밋 유실 0건 | 관측 결과이며 서비스의 절대 보증은 아님 |
| HA 복구 목표 | 사용자 관점 복구 ≤ 120초 | 제안 목표이며 AWS 보장값은 아님 |
| 복원 목표 | 복원 후 정상 업무 가능까지 ≤ 60분, 관측 복원 가능 시점 지연 ≤ 5분 | S/L별로 보고; 실제 요구에 맞춰 변경 가능 |

**SLO 용량(Q)** 은 모든 요청 유형의 기준과 오류율을 3회 모두 만족하는 최대 관측 도착률이다. 제품 SLO가 정해지면 데이터를 보기 전에 기준을 수정한다. 기준 미달은 실패 원인과 함께 기록하며 임의로 완화하지 않는다.

### DSQL 비교의 공정성과 결과 표현

- **공통 업무 비교:** 동일한 데이터·업무 결과·불변식·요청률·재시도 상한을 유지한다. E001에서 검증한 공통 SQL과 명시적 REPEATABLE READ를 기준으로 시작하되, 격리 수준 이름만으로 동작이 같다고 보지 않고 E004로 검증한다. 원본 PostgreSQL 스키마와 DSQL용 변경을 함께 보존하며 미지원 기능을 숨기기 위해 제약을 제거하지 않는다.
- **실용 구성 비교:** 공통 업무를 충족한 뒤 서비스별 권장 튜닝·SQL·풀링을 적용하는 별도 조건을 둔다. 조정 항목과 투입 시간 상한을 사전에 정하고, 성능 이득과 변경·유지보수 부담을 함께 기록한다. 한 서비스에만 튜닝 시간을 추가한 결과를 기본 성능으로 표현하지 않는다.
- 업무를 정합성까지 동등하게 구현할 수 없는 조건은 **적용 불가**로 남긴다. 지원되는 다른 업무의 비교는 계속하되 부분 성공을 전체 서비스 우위로 일반화하지 않는다.
- 서비스 B별로 `처리량 배율 = Q_D1 / Q_B`, 같은 요청률의 `p99 비율 = p99_D1 / p99_B`, `비용 비율 = 성공 업무당 비용_D1 / 성공 업무당 비용_B`를 보고한다. 처리량 배율은 클수록, 지연·비용 비율은 작을수록 유리하다. 분모 0·미측정·요구 미달은 비율 대신 사유를 표시한다.
- 최소 비교는 제안 시작 구성 간 결과다. 대조군 증설 없이 측정한 DSQL 처리량을 비용이 같은 성능이나 기존 서비스의 최대 성능으로 해석하지 않는다. 필요 시 같은 SLO를 만족하는 최소 관측 비용 구성을 제한된 추가 탐색으로 찾는다.

### 개발·운영 편의성의 측정

편의성은 E011에서 동일한 작업 목록으로 평가하고 E001/E003/E007/E008/E009의 기록을 재사용한다. 첫 시도의 학습·문제 해결과 문서화 후 재실행을 분리하며, 같은 수행자·자동화 수준으로 서비스 순서를 바꿔 측정한다. 익숙한 서비스와의 숙련도 차이를 한계로 남긴다.

| 작업 | 공통 완료 조건 | 기록할 지표 |
| --- | --- | --- |
| 생성·인증·첫 거래 | 새 환경에서 최소 권한 앱 역할로 첫 주문 성공 | 전체 경과 분, 실제 작업 분, 수동 단계·설정 항목 수, 인증 오류 |
| 스키마·앱 이관 | 동일 업무와 정합성 검사 통과 | 수정 SQL·설정·코드의 항목 수와 diff, 수정·디버깅 분, 잔여 제약 |
| 급증·성장 대응 | 목표 요청률에서 SLO 충족 | 개입 횟수·작업 분, 용량/풀/쿼터 조정, SLO 위반 시간 |
| 스키마 변경·진단 | 인덱스 사용 가능 및 주입한 오류 원인 확인 | 작업 분·대기 분, 명령 수, 지표/로그 부족으로 미해결된 항목 |
| 복구·재연결 | 데이터 검증 후 앱 정상 동작 | RTO, 작업 분, 권한·연결 설정 수정, 기능 공백 |
| 전체 삭제 | 해당 실험의 잔여 리소스 0개 확인 | 작업 분·대기 분, 실패/재시도·누락 수 |

단순 명령 수는 스크립트 포장 여부에 좌우되므로 독립적인 판단·설정·수동 개입도 함께 센다. 기능이 없어서 수행하지 못한 작업은 0분이 아니라 `적용 불가`, 서비스가 대신 관리하는 작업은 `사용자 작업 불필요`와 확인 근거를 남긴다. 실험하지 않은 운영 부담 감소를 절감 시간으로 환산하지 않는다. 작업별 `절감 분 = B의 실제 작업 분 - D1의 실제 작업 분`을 비교하며, 도입 시 변경 비용과 반복 운영 작업의 차이를 분리해 결론 낸다.

## 5. 개별 실험

<a id="e001"></a>
### E001 — SQL 호환성과 실행 가능성

- **대상/우선순위:** D1/R1/A1/A2 / P0; R2는 선택 확장.
- **질문·가설:** 기존 PostgreSQL 업무를 DSQL에서도 같은 결과와 정합성으로 실행할 수 있는가? SQL 수정뿐 아니라 인증·DDL·트랜잭션 처리의 변경 작업이 필요할 수 있다.
- **방법:** PK/FK/CHECK/UNIQUE, sequence/identity, UPSERT, JOIN/CTE/window, JSONB와 인덱스, 임시 테이블, partition, trigger/function, `SELECT FOR UPDATE`, 격리 수준, 취소·timeout을 각각 검사한다. 실제 선택할 ORM의 스키마 변경 SQL과 논리 복제/CDC 필요 기능도 지원표로 남긴다.
- **측정/판정:** 항목별 성공·거부·결과 차이, SQLSTATE, 필요한 수정과 운영 권한, 수정·디버깅 시간. 공통 업무의 필수 제약과 결과가 맞아야 해당 업무의 본 성능 실험으로 진입한다. 원본 그대로/동등한 업무로 수정 가능/적용 불가를 구분한다. D1의 준비를 뒤로 미루고 기존 서비스만으로 비교를 완료하지 않는다.
- **산출물:** 기능 지원표, 검증 SQL, 버전·설정 명세, 재현 명령, 이관 변경 목록과 E011 작업 기록. D1의 미지원 기능을 다른 서비스에서도 제거해 결과를 유리하게 만들지 않는다. 현재 SQL 지원·트랜잭션 한도는 실행 직전 공식 문서와 실제 SQL로 확인한다. [SQL 지원](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-postgresql-compatibility-supported-sql-features.html), [이관 시 고려사항](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-postgresql-compatibility-migration-guide.html)

<a id="e002"></a>
### E002 — OLTP 처리량과 지연의 한계

- **대상/우선순위:** D1/R1/A1/A2 / P0; R2는 선택 확장.
- **질문·가설:** DSQL은 각 대조군보다 같은 부하의 지연과 SLO 용량이 얼마나 다른가? 분산 구조의 고정 지연과 확장 이득은 부하에 따라 다를 수 있다.
- **방법:** S·기본 OLTP로 시작한다. 예비 측정에서 SLO를 만족한 성공 TPS의 구성별 최댓값을 구한 뒤, 그중 가장 작은 값을 임시 기준 `Qref`로 고정한다. 최소 비교는 모든 서비스에 같은 0.5/1.0/1.2 × Qref를 입력한다. 필요 시 0.8배를 추가한다. 각 서비스의 경계가 안 보이면 도착률을 25%씩 올리고 경계 주변을 좁힌다. 충분한 클라이언트·발생기 용량을 유지한다. 예비 조건이 모두 실패하면 도착률을 낮추거나 원인을 해결한 뒤 본 측정에 진입한다.
- **확장:** 읽기 중심/쓰기 중심, L 데이터는 필요한 분기부터 추가한다. 각 서비스의 용량 측정과 같은 도착률에서의 비교를 모두 보고한다.
- **측정/판정:** 요청 유형별 p50/p95/p99, SLO 용량, 성공 TPS, 최종 오류, 포화 원인, 각 대조군 대비 D1의 비율. 세 번의 본 측정이 통과한 조건만 Q로 채택한다. 예산 내 경계에 도달하지 못하면 Q는 최대 용량이 아닌 관측 하한임을 표시한다.

<a id="e003"></a>
### E003 — 연결 수, 연결 폭증, 풀링

- **대상/우선순위:** D1/R1/A1/A2 / P0; E001의 기본 연결 확인 후 E002와 연계.
- **질문·가설:** 앱 인스턴스가 늘 때 연결 관리가 DB보다 먼저 병목이 되는가? 적정 풀링은 급증 피해를 줄이지만 대기열을 만들 수 있다.
- **방법:** 지속 연결과 요청마다 새 연결을 비교한다. 앱 동시 요청을 16/64/256/1,024로 바꾸되 1,024는 가상 사용자이며 DB 연결은 설정 한도로 제한한다. DB 총 풀 크기 16/64/128, 60초간 평소의 10배 연결 시도를 비교한다.
- **분리 조건:** 직접 연결 → 앱 연결 풀 → 지원되는 구성의 RDS Proxy 순으로 한 요소씩 바꾼다. Proxy 지원 버전·세션 pinning·비용을 기록한다. Proxy 결과를 직접 연결 기준선에 섞지 않는다.
- **DSQL 검사:** IAM 토큰 생성·갱신, 새 연결의 인증, 장기 풀의 연결 교체와 연결률 제한을 검사한다. 토큰 유효기간과 이미 맺어진 연결의 수명을 구분하고, 실제 계정 쿼터를 기록한다. 원시 드라이버와 공식 커넥터를 비교한다면 별도 조건으로 기록하고 다른 서비스에도 동등한 풀링·인증 지원을 적용한다. [인증](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/authentication-authorization.html), [연결 제한](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/CHAP_quotas.html)
- **측정/판정:** TLS/인증·연결 p95/p99, 풀 대기, 실제 DB 연결 수, 거부율, 메모리, 정상 부하 복귀 시간. E002와 같은 요청률·SLO를 만족하는 최소 풀과 최대 앱 동시성을 찾는다.

<a id="e004"></a>
### E004 — 트랜잭션 경합과 데이터 정합성

- **대상/우선순위:** D1/R1/A1/A2 / P0; 본 OLTP 비교 전 핵심 정합성 검사를 통과한다.
- **질문·가설:** 인기 상품과 동시 갱신에서 오류·재시도는 얼마나 늘며 업무 불변식을 지킬 수 있는가?
- **방법:** 균등/인기 키 집중 부하, 동시성 16/64/256을 비교한다. barrier로 경쟁 트랜잭션의 읽기·갱신 순서를 제어해 lost update, write skew, 이중 주문, 교착을 재현한다. READ COMMITTED / REPEATABLE READ / SERIALIZABLE을 지원되는 범위에서 각각 검사한다.
- **DSQL 비교:** 공통 REPEATABLE READ 조건에서 D1의 OCC 충돌·SQLSTATE와 대조군의 잠금 대기·오류를 비교한다. `SELECT FOR UPDATE`의 지원을 동일한 행 잠금 동작으로 취급하지 않는다. 미지원 격리 수준은 적용 불가로 표시하고 각 서비스 기본 격리 수준의 결과는 별도 표로 남긴다. [DSQL 동시성 제어](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-concurrency-control.html)
- **정합성 검사:** 재고 음수 0건, 확정 주문 수량과 재고 변동 일치, 합성 계정 간 이체 전후 총액 보존, 업무 ID별 효과 1회, 중단된 거래의 부분 반영 0건. 격리 수준에서 허용되는 현상과 구현 오류를 구분한다.
- **재시도:** 없음/최대 3회 시도, 총 마감 2초, 지수 backoff+jitter를 고정한다. 접속이 끊겨 커밋 여부가 불명확하면 업무 ID를 조회한 뒤 재시도한다.
- **측정/판정:** 원시 충돌률, 최종 실패율, 재시도 포함 지연·TPS·비용. 불변식 위반이 있으면 해당 구현으로 성능 우위를 주장하지 않는다.

<a id="e005"></a>
### E005 — 읽기 확장과 최신 데이터 가시성

- **대상/우선순위:** D1/A1/A2, 선택적 R2/R1+RR / P1.
- **질문·가설:** DSQL의 단일 접속 경로는 기존 서비스의 reader 라우팅 대비 읽기 성능·최신성과 앱 설정 부담이 어떻게 다른가?
- **방법:** 동일한 읽기 중심 트래픽을 writer 전용/reader 분산으로 보낸다. R2의 reader 1개 사용/2개 사용을 비교하고, Aurora의 두 번째 reader 추가는 별도 비용 조건으로 측정한다. 연결별 실제 대상 노드와 분배를 기록한다.
- **DSQL 조건:** 같은 읽기 트래픽을 D1 클러스터 엔드포인트에 보낸다. reader 수를 맞추거나 DSQL 내부 복제 지연을 추정하지 않는다. 읽기/쓰기 라우팅과 최신성 보완에 필요한 앱 설정·코드 변경은 E011에 합산한다.
- **가시성:** 업무 ID·버전의 커밋 성공을 확인한 뒤 별도 연결의 새 트랜잭션으로 조회한다. D1 엔드포인트와 대조군 writer/reader를 구분하고 오래 열린 snapshot의 읽기를 복제 지연으로 세지 않는다. 같은 클라이언트의 monotonic clock으로 최초 확인까지 시간을 재고, 조회 간격 10 ms와 측정 상한 5초에 따른 오차·검열 값을 표시한다.
- **측정/판정:** 읽기 TPS/p99, writer commit 지연, 애플리케이션 관측 stale-read 비율·시간, 엔진 복제 지표. 쓰기 직후 조회를 writer로 보내는 정책도 비교한다. 최신성 100 ms/1초 허용 시나리오를 각각 보고한다.

<a id="e006"></a>
### E006 — 연결 장애·서비스별 복구와 성공 커밋의 보존

- **대상/우선순위:** D1/R1/A1/A2 / P0의 공통 연결 장애; 서비스별 failover는 P1 확장.
- **질문·가설:** DB 전환 시간보다 DNS·연결 풀·재시도를 포함한 업무 복구가 늦어질 수 있다.
- **공통 방법:** 공통으로 감당 가능한 Q의 30%/80% 부하에서 부하 발생기의 해당 실험 DB 연결만 30초 차단해 재연결·중복 처리를 검사한다. 조건당 3회, 차단 전 10분·후 최소 10분을 관측한다. 이는 클라이언트 통신 장애이며 DB/AZ 장애나 DSQL 내부 자동 복구를 검증한 것으로 해석하지 않는다.
- **서비스별 분기:** R1은 reboot with failover, A1/A2와 선택적 R2는 지원되는 cluster failover를 별도로 수행한다. D1에 대응하는 공개 장애 주입 수단이 없으면 해당 행은 `미측정`으로 남긴다. 서로 다른 장애를 같은 강도의 장애라고 가정해 RTO 순위를 매기지 않는다. [RDS 전환 동작](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_RebootInstance.html), [DSQL 구조](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/what-is-aurora-dsql.html)
- **측정:** 마지막 정상 완료, 장애 시작, 최초 정상 완료, 이후 60초 동안 SLO를 유지하기 시작한 시각. API 완료 시간, DNS 변경, reconnect, timeout·재시도 건수를 분리한다.
- **데이터 검증:** DB 밖에 보존한 요청/성공 응답 기록을 복구된 `operation_receipts`와 대조한다. 성공 확인/확정 실패/커밋 불명확을 구분하고 모든 불명확 거래를 조정한다. 성공 확인 후 누락된 건수·시간 범위를 관측 RPO로 보고한다.
- **판정:** 성공 확인 거래 유실과 중복 효과 0건, 목표 복구시간 충족 여부. 3회 무손실을 절대적 무손실 보증으로 일반화하지 않는다.

<a id="e007"></a>
### E007 — 백업, 시점 복원, 실수 복구

- **대상/우선순위:** D1/R1/A1/A2 / P1; S의 복구 작업을 먼저 평가하고 L은 필요 시 확대.
- **질문·가설:** 백업 존재 여부보다 복원된 앱이 정상 업무를 처리하기까지의 시간이 중요하다.
- **RDS/Aurora PostgreSQL 방법:** 보존 기간 7일로 설정하고 1초 간격의 검증 마커를 쓰면서 정상 부하를 실행한다. 정상 기준 시각 뒤 합성 주문 데이터를 잘못 변경한 상황을 만든다. 복원 가능 시각을 확인한 뒤 오류 직전으로 새 DB/클러스터를 복원한다. snapshot 복원도 별도 조건으로 비교한다. [RDS PITR](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_PIT.html), [Aurora 백업/복원](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Managing.Backups.html)
- **DSQL 조건:** D1은 AWS Backup의 지원되는 복구 지점에서 별도 클러스터로 복원하고 인증·앱 연결을 복구한다. 위 PITR 절차를 그대로 적용하지 않으며, 오류 이전의 특정 시점 복원 요구를 충족할 수 없으면 기능 공백으로 기록한다. 서비스별 복구 지점 간격·보존 설정·권한 차이와 실제 작업 시간을 E011에 남긴다. [DSQL 복원](https://docs.aws.amazon.com/aws-backup/latest/devguide/restore-auroradsql.html)
- **측정:** 복원 요청→리소스 준비→접속 가능→데이터 검증→정상 부하 SLO 회복 시간을 분리한다. 복원 가능 최신 시각의 지연, 원본 부하의 지연 변화, 복원 후 최초 접근 지연도 남긴다.
- **판정:** 오류 전 마커·업무 불변식·집계 해시가 기대 상태와 일치하고 오류 이후 변경은 제외되는지 검사한다. 서비스 전환과 필요한 권한·파라미터·연결 설정 복구까지 RTO에 포함한다. snapshot과 PITR의 복구 시점 차이를 구분한다.
- **반복/정리:** 방식·크기별 3회. 7일은 실험 중 보존 정책이며 실험 후 보관 허용이 아니다. 복원 DB/클러스터·snapshot·AWS Backup 복구 지점 등 실험용 리소스를 종료 즉시 모두 삭제하고 잔여 0개를 확인한다. 즉시 삭제를 막는 보존 잠금은 실험 리소스에 설정하지 않는다.

<a id="e008"></a>
### E008 — 데이터 증가와 운영 작업의 영향

- **대상/우선순위:** D1/R1/A1/A2 / P1; 인덱스 생성·기본 진단 작업은 E011의 P0 범위.
- **질문·가설:** DSQL에서 직접 관리할 용량·유지보수 작업이 줄더라도 DDL·트랜잭션 제한과 관측 도구 차이가 업무를 어렵게 만들 수 있는가?
- **방법:** 먼저 공통 Q의 60%에서 인덱스 생성·지원되는 스키마 변경을 한 번에 하나씩 수행한다. 필요 시 4시간 갱신·삭제 부하를 3회 수행해 성장·장기 운영을 조사한다. 작업 없는 구간을 대조군으로 두고 큰 JOIN/정렬/집계를 분리한다. 5분 장기 트랜잭션은 지원 범위에서만 시도하며 D1의 제한 초과 거절도 결과로 남긴다. RDS/Aurora의 정상 autovacuum을 끄지 않고 D1에 해당 제어를 가정하지 않는다.
- **DSQL 조건:** DDL/DML 제약, 비동기 인덱스 생성의 완료 확인과 사용 가능 시점, 스키마 변경 중 충돌·재시도를 측정한다. 직접 실행할 수 없는 유지보수는 `사용자 작업 불필요` 또는 `적용 불가`를 구분한다. 진단 지표 부족을 운영 작업 0분으로 기록하지 않는다. [SQL 지원](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-postgresql-compatibility-supported-sql-features.html), [트랜잭션 한도](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/CHAP_quotas.html)
- **별도 운영 분기:** 고정 용량 클래스 증설, RDS 스토리지 증설, 지원되는 minor upgrade 경로를 각각 계획하고 사전·사후 동일 부하를 재생한다. A2의 용량 변화는 E009로 분리한다. 각 변경은 동일 초기 상태의 별도 실행으로 다룬다.
- **측정/판정:** 작업 완료 시간, lock wait, dead tuples, 테이블/인덱스 크기, WAL·임시 파일, 디스크 여유, query plan, SLO 위반 시간·실패율·운영자 작업 시간. 업그레이드 후 E001/E004 핵심 검사를 다시 통과해야 한다.

<a id="e009"></a>
### E009 — DSQL·기존 서비스의 급증 대응과 유휴 후 재개

- **대상/우선순위:** D1/R1/A1/A2 / P0; 서버리스 간 핵심 비교는 D1 대 A2.
- **질문·가설:** DSQL은 기존 서버리스보다 급증·유휴 후 재개를 얼마나 잘 처리하며, 용량 계획·설정·개입을 얼마나 줄이는가?
- **방법:** D1은 서비스 관리 용량, R1/A1은 E002의 고정 구성, A2는 4–32 ACU 자동 조절로 시작한다. A2의 0–32 ACU auto-pause는 유휴 분기의 별도 조건이며, 16–16 ACU는 원인 분리가 필요할 때만 추가한다. 고정 ACU를 A1과 동등 CPU라고 보지 않는다.
- **트래픽:** 20% Qref 10분 → 200% 2분 → 100% 20분 → 5% 10분. 같은 절대 요청률을 네 서비스에 적용해 3회 반복한다. 대조군의 관측 용량과 D1의 제공 지표·DPU·클라이언트 지연을 시계열로 기록한다. 우선 개입 없이 비교하고, 수동 증설·쿼터 조정이 필요하면 별도 조건으로 작업 시간을 측정한다.
- **유휴 분기:** 모든 서비스에서 연결 풀·SQL 상태 탐침을 닫고 연결 없는 유휴 15분 후 첫 업무를 실행하는 주기를 5회 반복한다. R1/A1은 가동 상태를 유지한다. A2는 지원 버전에서 pause 대기 300초로 설정하고 외부 메트릭으로 실제 pause를 확인한다. D1에 A2의 pause/resume 구조를 가정하지 않으며 동일한 유휴 후 관측값만 비교한다. RDS Proxy가 포함된 A2는 별도 조건으로 구분한다. [자동 pause 조건](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/aurora-serverless-v2-auto-pause.html)
- **측정/판정:** 급증 후 SLO 복귀시간, 초과 지연·timeout, 첫 연결·첫 업무 지연, 실제 작업 분·수동 개입 횟수, 구간별 비용. A2는 ACU-hours·pause 여부, D1은 DPU·저장 비용을 남긴다. 상시 서비스 기준과 유휴 허용 서비스 기준을 나눠 DSQL의 성능·편의성·비용 차이를 보고한다.

<a id="e010"></a>
### E010 — 같은 업무의 비용과 선택 기준

- **대상/우선순위:** 모든 실행 구성 / P0; 비용 수집은 첫 실험부터, 종합 판단은 마지막.
- **질문·가설:** 시간당 단가가 낮아도 같은 SLO·HA 요구를 만족하는 총비용은 더 높을 수 있다.
- **방법:** 24시간 일정 부하, 8시간 활성+16시간 유휴, E009 급증 패턴을 각각 정의하고 각 패턴을 모든 후보에 동일하게 적용한다. 측정 구간을 실제로 실행하지 못한 경우 월 추정치로 표시하고 부하·가동 시간·선형 환산 가정을 공개한다.
- **비용 범위:** D1의 DPU·스토리지와 대조군 전체 writer/standby/reader·ACU·스토리지·I/O, 모든 서비스의 백업/복원·Proxy·네트워크·모니터링. 부하 발생기와 실험 자동화 비용은 실험 총비용에 별도 합산한다. 리전·통화·단가 확인일·가격 출처·실제 사용량을 기록한다. D1의 DPU를 CPU 사용 시간이나 성공 요청 수와 동일시하지 않고 재시도를 포함한 실제 사용량을 수집한다. [DSQL 과금](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/billing-metering.html)
- **선택적 I/O 분기:** 기본 비교 후 비용 결론에 필요하면 A1의 Standard/I/O-Optimized를 같은 초기 데이터·부하로 비교하고 필요한 경우 A2도 확장한다. Standard→I/O-Optimized 전환은 30일 제약이 있으므로 반복 토글을 전제로 하지 않고 별도 클러스터 또는 독립된 재생 구간을 설계한다. [Aurora 스토리지 구성](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.StorageReliability.html)
- **계산:** `성공 업무 100만 건당 비용 = 같은 구간 DB 운영비 / 완료된 고유 업무 수 × 1,000,000`. 기한 내 성공 업무 기준도 함께 보고한다. 성공 0건은 나눗셈 대신 비용과 실패만 보고한다. 재시도·유휴·생성/정리 시간 비용을 누락하지 않는다.
- **편의성과의 연결:** 서비스별 일회성 이관·도구 구현 시간과 반복 운영 시간을 E011에서 가져와 비용 표 옆에 별도 표시한다. 사람의 시간은 단가가 정해지기 전에는 금액으로 합산하지 않는다. 운영 횟수·인건비 가정 없이 DSQL의 총소유비용 절감액을 만들지 않는다.
- **판정:** 온디맨드·할인 전 비용을 기준선으로 삼고 크레딧·약정 할인은 별도 표기한다. 실제 청구액은 확인 전까지 미확인으로 둔다. 기능·정합성·SLO·HA 요구를 충족한 후보끼리 비용을 비교한다. [RDS 가격](https://aws.amazon.com/rds/postgresql/pricing/), [Aurora 가격](https://aws.amazon.com/rds/aurora/pricing/)

<a id="e011"></a>
### E011 — DSQL의 개발·운영 편의성과 도입 부담

- **대상/우선순위:** D1/R1/A1/A2 / P0; 최초 생성부터 삭제까지 지속해서 기록한다.
- **질문·가설:** DSQL에서 줄어드는 인프라 운영 작업이 SQL·인증·재시도·진단 방식의 변경 부담을 감수할 만큼 큰가? 초기 이관과 반복 운영에서 결론이 다를 수 있다.
- **동일 작업:** 생성→최소 권한 인증→스키마 적용→첫 주문→풀 재연결→인덱스 생성→오류 원인 진단→트래픽 변화 대응→전체 삭제를 네 서비스에서 수행한다. 복구 작업은 E007을 실행한 뒤 보완한다. 수행하지 않은 복구·장기 유지보수는 미측정으로 남기며 최소 비교만으로 편의성 전체를 평가하지 않는다.
- **이관 검사:** 하나의 기준 PostgreSQL 앱·마이그레이션을 먼저 고정하고 서비스별 diff를 관리한다. E001/E004의 SQL·트랜잭션 변경, IAM 토큰 처리, 연결 수명·풀 설정, ORM 지원이 필요할 경우 그 수정까지 기록한다. 이미 자동화된 작업도 자동화 구현·검증 시간을 일회성 비용으로 공개한다. [이관 안내](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-postgresql-compatibility-migration-guide.html), [인증](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/authentication-authorization.html)
- **진단 검사:** 동일한 권한 오류와 경합 부하를 서비스별로 만들고 원인·대응을 찾아 정상 업무를 복구한다. 지표·로그·실행 계획의 접근성, 필요한 도구와 권한, 미해결 항목을 기록한다. DSQL의 미공개 내부 지표를 무조건 실패로 보지 않고 실제 문제 해결 가능 여부로 평가한다.
- **반복/측정:** 첫 구현·학습 시간은 1회 별도 기록하고, 절차가 고정된 작업은 독립 3회 재실행한다. E001/E003/E006/E008/E009와 같은 실행 ID를 재사용해 중복 실행·비용 합산을 피한다. 전체 경과 시간, 실제 작업 시간, 서비스 대기 시간, 수동 개입·설정 항목, 수정량과 실패를 작업별로 남긴다.
- **판정:** 작업 완료·정합성·SLO가 동등한 경우에만 시간 절감률을 산출한다. `작업별 절감률 = (B의 작업 시간 - D1의 작업 시간) / B의 작업 시간 × 100`이며 B가 0분이면 절대 차이만 보고한다. 인프라 관리에서 줄어든 작업과 애플리케이션에 추가된 부담을 나란히 제시하고 임의 가중치의 종합 점수는 만들지 않는다.

<a id="e012"></a>
### E012 — DSQL의 대용량 조회·집계와 OLTP 간섭

- **대상/우선순위:** D1/R1/A1/A2 / P1, 업무 우선순위 3단계. R2/R1-NVMe/A1-IO/A3/A3-IO는 선택 확장.
- **질문·가설:** DSQL은 큰 JOIN·집계에서도 대조군 대비 어떤 지연·비용을 보이며, OLTP와 함께 실행할 때 어떤 간섭과 튜닝 부담이 생기는가?
- **부하:** L 데이터의 고객별 최근 주문 JOIN, 기간별 매출 GROUP BY, 상품 순위 window+정렬, JSON 조건 조회를 정의한다. 선택도 0.1%/10%/전체 스캔을 구분하고 지원되는 인덱스·통계를 고정한다. `work_mem` 등 제어 가능한 설정은 기록하되 D1에 같은 제어를 가정하지 않는다. 동일 쿼리 결과의 행 수와 정규화 해시를 검증하며 미지원 쿼리·timeout도 결과로 남긴다.
- **방법:** 먼저 집계 단독 동시성 1/4/16으로 탐색한다. 이후 동일 쿼리 도착률을 고정해 20분씩 3회 측정한다. OLTP를 공통 Q의 60%로 유지한 상태에서도 재생한다. 기본 비교는 D1 엔드포인트와 대조군 writer에서 함께 실행하고, reader 분리는 지원 구성의 별도 조건으로 실제 reader 수·전체 비용·라우팅 작업을 기록한다.
- **선택적 원인 분리:** 기존 서비스의 튜닝이 DSQL 채택 결론을 바꿀 가능성이 있을 때만 Aurora의 NVMe 유무 × Standard/I/O-Optimized 2×2 조건과 RDS의 임시 tablespace 설정을 확대한다. 첫 접근과 반복 접근을 따로 측정하고, 지원되는 임시 파일·캐시·I/O 지표 및 실행 계획을 수집한다.
- **측정/판정:** 쿼리 유형별 p50/p95/p99, 완료 쿼리/분, 읽은 행·바이트, 임시 공간, 취소율, OLTP p99/오류율 변화, 완료 쿼리당 비용. 집계의 제안 목표는 p95 ≤ 5초, 최종 오류 ≤ 0.1%, timeout 30초이며 OLTP는 기존 기준을 유지한다. 지연이나 자원 사용이 줄어도 결과 불일치는 불합격이다.
- **범위:** 전체 조합을 곱하지 않고 가장 느린 대표 쿼리와 간섭이 큰 조건부터 확대한다. 표본이 적으면 p99의 표본 수와 불확실성을 명시한다. Redshift 등으로 분석을 분리하는 평가는 후속 범위다.

## 6. 실행 순서와 실험량 제한

| 단계 | 실행 범위 | 다음 단계 진입 조건 |
| --- | --- | --- |
| 준비·첫 사용 | D1/R1/A1/A2의 E001 및 E004 핵심 검사, E011 작업 기록·E010 비용 수집 시작 | DSQL을 포함한 동일 업무·정합성 검증, 지원 차이·명세·수집 검증 완료 |
| 1순위 OLTP 최소 비교 | 네 구성에서 S·기본 OLTP의 E002, E003 기본 풀·재연결, E004 경합, E006 공통 연결 장애 | DSQL의 지연·SLO 용량·재시도·인증 부담을 대조군별로 설명 가능 |
| 2순위 급증·유휴 | 네 구성의 E009, E003 연결 급증; E011의 용량 대응 기록 | DSQL 대 A2 및 고정 용량 대조군의 동일 트래픽 결과·개입·비용 확보 |
| 최소 비교 종합 | E008 인덱스 생성·진단, E011 기본 작업·삭제, E010 구간 비용 | DSQL 성능 비율·작업 시간 차이·이관 변경량과 제한을 함께 제시 |
| 운영 검증 확대 | E005/E007/E008, E006 서비스별 장애 전환, 필요한 경우 R2 | 최신성·복구·장기 운영에서 미측정인 편의성·제약 보완 |
| 3순위 조회·집계 | D1/R1/A1/A2의 E012, 필요 시 NVMe·스토리지 변형 | DSQL의 큰 쿼리 적합성과 OLTP 간섭·비용·튜닝 부담 설명 가능 |
| 최종 판단 | E010/E011 갱신, 대조군별 선택표 | DSQL이 유리·불리한 조건과 미측정 범위, 전체 삭제 확인 |

전체 조합을 처음부터 실행하지 않는다. 예를 들어 E002만 해도 4개 구성 × 3개 업무 비율 × 2개 크기 × 4개 도착률 × 3회 × 30분 = **구성 가동 시간 144시간**이다. DSQL은 요청 기반, 대조군은 전체 노드·ACU와 스토리지 등으로 과금되므로 구성 가동 시간을 인스턴스 시간이나 동일 비용으로 해석하지 않는다. 생성·적재·초기화·대기·복원 시간은 별도다.

최소 비교의 E002는 D1/R1/A1/A2의 4개 구성 × 1개 업무 비율 × 1개 크기 × 3개 도착률 × 3회 × 30분 = **18시간**이다. 이는 E002의 본 측정·워밍업만 계산한 값이며 예비·경계 탐색, E003/E004/E006/E009/E011 및 생성·정리 시간은 포함하지 않는다. 예산이 작으면 업무·데이터·선택 분기를 줄이되 DSQL 또는 기존 서버리스 대조군을 빼고 핵심 비교를 완료했다고 하지 않는다.

예산이 지정되지 않았으므로 금액을 임의 확정하지 않는다. 실행 배치마다 `조건 수 × 반복 수 × (준비+측정+복원 시간)`과 전체 토폴로지 단가·보관 비용을 계산한다. DSQL은 소규모 예비 실행의 업무별 DPU 사용량으로 측정 구간 비용을 추정하고 고정 시간당 단가를 가정하지 않는다. 활성 DB는 가급적 한 비교 구성씩 운영하되 반복의 서비스 순서를 바꾼다. 시간/비용 상한·최대 추가 탐색 횟수·정리 담당을 배치 명세에 기록한 후 실행한다. E010의 24시간 패턴은 최소 비교의 필수 가동 시간이 아니며 단기 실측과 가정 기반 환산을 먼저 제시한다.

무한 경계 탐색을 피하기 위해 최종 기술 실패율 5% 초과 또는 제공되는 DB 디스크 여유 지표가 20% 미만으로 60초 지속되면 해당 부하 구간을 종료한다. DSQL의 미공개 디스크 여유는 추정하지 않고 쿼터 초과·서비스 오류·비용 상한으로 제한한다. 고장 주입과 E003/E009의 시간 제한된 급증 구간에는 오류율 중단 기준을 적용하지 않고 예정된 관측 시간을 따른다. 자원·시간·예산 상한은 계속 적용한다. 실시간 사용량 추정과 AWS 청구 반영 지연을 고려해 상한에 정리 여유를 두고, 도달 시 현재 부하를 중단하고 추가 실행 없이 정리한다. 중단된 조건도 실패/미완료 결과로 보존한다.

성공·실패·중단 여부와 관계없이 각 실험·배치 종료 즉시 모든 실험용 리소스를 삭제한다. 다음 실험을 위해 DB를 유휴 상태로 남기지 않는다. 측정 원본은 로컬 `artifacts/`에 저장한 뒤 DSQL/RDS/Aurora 클러스터, 부하 발생기, 네트워크, 모니터링, snapshot·보존 백업·복구 지점 등 부속 리소스까지 삭제한다. 계정·리전·ID별 실제 삭제와 잔여 0개를 검증·기록해야 `completed`로 처리한다. 상세 절차는 [프로젝트 규칙](AGENTS.md#cost-control--mandatory-cleanup)을 따른다.

## 7. 구현 준비와 기록 형식

실행할 실험부터 `experiments/NNN-topic/`를 만들고 [실험 템플릿](templates/experiment.md)에 다음을 채운다. 아래 파일은 앞으로 구현할 산출물이며 현재 실행 가능한 명령이 있다는 뜻은 아니다.

- `README.md`: 질문·가설·판정 기준, 준비→적재→워밍업→측정→검증→정리 명령.
- `config/`: 엔진/패치, 전체 토폴로지, AZ, 스토리지, 파라미터, 도구 버전, 트래픽·재시도 정책, 시간/비용 상한.
- `sql/` 및 실행 도구: 스키마, 결정적 데이터 생성, 업무 쿼리, 정합성 검사, 실패 주입·복구 절차. 공유 도구는 재사용하되 커밋과 버전을 고정한다.
- `results/`: 검토한 요약·작은 비식별 측정 자료·그래프. 실행 ID별 구성, 성공 TPS, 지연, 오류, SLO 달성, DSQL/대조군 비율, 비용, 작업 시간·수정량과 해석 한계를 남긴다.
- `artifacts/`: Git에서 제외되는 요청 로그·히스토그램·메트릭·이벤트·실행 계획·작업 기록 원본을 로컬에 보관한다. 경로와 checksum을 요약에 기록하며 원본 보관을 이유로 실험용 클라우드 리소스를 남기지 않는다.

실행 ID는 `YYYYMMDDTHHMMSSZ-E002-R1-r01`처럼 UTC·실험·구성·반복을 포함한다. 각 실행에 코드 커밋, 시작/종료 UTC, 단조 시계의 측정 구간, 데이터 시드/행 수, 초기 상태, 배치 ID를 연결한다. 명령·로그에는 비밀번호·토큰·개인정보를 남기지 않는다.

수집 도구는 성공/실패/timeout/재시도/커밋 불명확 요청을 의도적으로 발생시켜 분류와 계산을 검증한다. 부하 발생기 증설 시 측정 결과가 변하는지도 확인한다. 리소스 생성·변경·삭제 자동화는 대상 태그와 명세를 사용하고, 정리 후 DSQL·DB·reader·Proxy·EC2·네트워크·snapshot·백업·모니터링 등의 삭제와 잔여 리소스 0개를 확인한다. 이후 반영되는 청구액은 정리 시각과 대조해 별도로 정산한다.

최종 결과는 **DSQL을 선택할 조건과 선택하지 않을 조건**을 대조군별로 제시한다. 표의 각 행에는 업무·부하·기능 요구, DSQL과 대조군의 처리량/지연, 성능 비율, 실제 작업 시간 차이, 이관·재시도 부담, 성공 업무당 비용, 정합성·복구 충족 여부와 미측정 범위를 포함한다. 단일 종합 점수나 전체 서비스 순위를 만들지 않고, 어떤 성능·편의성 이점이 어떤 비용·제약을 수반하는지 판단한다.
