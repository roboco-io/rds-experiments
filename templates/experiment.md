# NNN — 실험 제목

> 상태: planned

## 질문과 가설

- 확인할 질문:
- 가설:
- 가설의 판단 기준:

## 비교 조건

DSQL 비교에서는 비교군 A를 D1, 비교군 B를 해당 대조군으로 명시합니다. 공통 업무와 서비스별 변경·튜닝 조건을 구분합니다.

| 항목 | 비교군 A | 비교군 B |
| --- | --- | --- |
| 서비스 / 엔진 / 정확한 버전 | | |
| 리전 / AZ 구성 / 배포 방식 | | |
| 인스턴스 또는 용량 범위 / 복제본 수 | | |
| 스토리지 유형 / 크기 / IOPS / 처리량 설정 | | |
| 파라미터 / 확장 기능 / 연결 풀 설정 | | |
| 데이터 크기 / 스키마 / 인덱스 / 생성 시드 | | |
| 부하 도구·버전 / 발생기 사양·위치 | | |
| 쿼리 구성 / 읽기·쓰기 비율 / 동시성 | | |
| 워밍업 / 측정 시간 / 반복 횟수 / 캐시 상태 | | |
| 격리 수준 / 인증·연결 방식 / 재시도 정책 | | |
| 공통 업무 대비 수정·튜닝 항목 / 투입 시간 | | |

비교군 간 통제하지 못한 차이와 그 이유:

## 재현 절차

실행 가능한 명령과 필요한 설정을 아래 순서로 작성합니다.

1. 환경 준비 및 리소스 생성
2. 데이터 준비와 워밍업
3. 부하 실행과 지표 수집
4. 결과 저장
5. 리소스 정리 및 잔여 리소스 확인

## 실행 기록

- 실행 ID / 코드 커밋:
- UTC 시작·종료 시각:
- 원본 자료 보관 위치 / 식별 정보:
- 오류 또는 계획에서 벗어난 사항:

## 성능 결과

단위, 표본 수, 집계 방법을 함께 적고 반복 실행별 결과를 남깁니다.

| 실행 ID / 비교군 | 성공 TPS | p50 (ms) | p95 (ms) | p99 (ms) | 오류율 (%) |
| --- | --- | --- | --- | --- | --- |

관측한 CPU, 메모리, I/O, 연결 수, 복제 지연 등 보조 지표:

- 대조군 대비 DSQL의 SLO 용량 배율 / 동일 요청률의 p99 비율:
- 정합성·SLO 충족 여부 / 적용 불가·미측정 조건:

## 제약과 동작

| 항목 | 공식 문서의 제약·출처·확인 날짜 | 실험에서 관측한 동작 | 영향 |
| --- | --- | --- | --- |

## 개발·운영 편의성

같은 완료 조건·수행자·자동화 수준에서 측정하고 첫 구현·학습과 반복 실행을 분리합니다. 수동 단계는 독립적인 판단·설정·개입을 기준으로 세며, 기능 미지원은 0분으로 기록하지 않습니다. 다른 실험의 기록을 사용하면 실행 ID를 연결하고 중복 합산하지 않습니다.

| 작업 / 실행 ID / 서비스 | 완료 조건·결과 | 전체 경과 (분) | 실제 작업 (분) | 서비스 대기 (분) | 수동 개입 (회) | 수정 항목·근거 |
| --- | --- | --- | --- | --- | --- | --- |

- 대상 작업: 생성·인증·첫 거래 / 이관 / 확장 / 스키마 변경·진단 / 복구 / 전체 삭제
- 일회성 구현·학습 시간 / 반복 운영 시간:
- 대조군 대비 DSQL의 작업별 절감 분·절감률 / 추가된 앱 구현 부담:
- 숙련도 차이 / 사용자 작업 불필요·적용 불가·미측정 항목과 근거:

## 비용

- 가격 기준: 리전 / 통화 / 확인 날짜 / 공식 출처 / 구매 방식·할인 가정
- 계산 범위: DB, 부하 발생기, 스토리지, I/O, 백업, 네트워크, 모니터링 등
- 유휴 시간과 리소스 생성·삭제에 걸린 시간의 비용 포함 여부:

| 항목 | 사용량·단위 | 단가 | 예상 비용 | 실제 청구액 | 근거 |
| --- | --- | --- | --- | --- | --- |

같은 시간 구간의 포함 비용을 성공한 트랜잭션 수로 나누어 단위 비용을 계산합니다. 실제 청구액이 확인되지 않았으면 미확인으로 표시합니다. 월 비용을 환산한다면 가동 시간과 부하 가정을 별도로 적습니다.

## 결론과 한계

- 질문에 대한 답과 근거:
- DSQL이 유리·불리한 조건 / 성능·편의성 이점에 수반되는 비용·제약:
- 결과가 적용되는 조건과 일반화할 수 없는 범위:
- 다음 실험:

## 정리 기록

성공·실패·중단 여부와 관계없이 실험 종료 즉시 해당 실험을 위해 프로비저닝한 모든 리소스를 삭제합니다. 필요한 측정 결과와 로그는 삭제 전에 로컬 `artifacts/`에 저장합니다. 스냅샷·보존된 백업 등 부속 리소스까지 삭제하고 잔여 리소스가 0개임을 확인한 뒤에만 상태를 `completed`로 변경합니다.

- 생성·삭제 대상 목록 / 계정·리전·리소스 ID:
- 삭제 명령 / 실행 결과:
- UTC 정리 시작·완료 시각 / 완료 여부:
- 잔여 리소스 검증 명령 / 결과 (0개 확인):
- 삭제 실패 시 잔여 리소스 / 실패 원인 / 후속 조치 / 재검증 결과:
